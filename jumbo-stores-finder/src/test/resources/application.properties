# Test-specific properties for 'test' profile
# Override main application properties

# R2DBC configuration - will be set dynamically by PostgresContainerConfig
# spring.r2dbc.url is not set here to allow dynamic configuration

# Liquibase configuration for tests
spring.liquibase.enabled=true

# Disable management endpoints for tests
management.endpoints.web.exposure.include=
management.endpoint.health.enabled=false 